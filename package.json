{
  "type": "module",
  "name": "crud_express",
  "version": "1.0.0",
  "description": "",
  "main": "./build/server.js",
  "scripts": {
    "watch": "nodemon ./build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && npx tsc",
    "start": "npm run watch && npm run build && node build/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.0"
  }
}

